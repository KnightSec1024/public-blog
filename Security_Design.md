# 一、安全设计原则


# 二、常见的安全设计场景

## 2.1 关于幂等和防重访

### 幂等性
**核心思想**：**无论你调用接口多少次，产生的结果都是一样的**。它关注的是“**结果**”。
**举个例子**：你用“DELETE /user/123”删除了一个用户。第一次调用，用户被删除，成功；第二次调用，系统发现用户已不存在，返回“用户不存在”的报错，但系统整体的状态（用户已被删）并没有发生改变。这就是一个幂等的操作。
**典型应用场景**：订单支付（确保网络重试不会导致重复扣款）、状态更新（如将订单状态更新为“已发货”）。

**如何设计**：

- 使用全局唯一ID (Idempotent Key)：客户端在请求头中传入一个唯一的请求ID（如 Idempotent-Key）。服务端通过这个ID判断该请求是否已经处理过，如果处理过则直接返回之前的成功结果，不再重复执行业务逻辑。
- 乐观锁 (Optimistic Locking)：在更新数据时，通过版本号（Version）或状态机来控制。例如，只有当订单状态为“待支付”时，才执行“支付成功”的更新，否则直接返回错误。

> HTTP GET、PUT、DELETE 在语义上天然幂等，POST 不是。


**幂等token**

客户端生成UUID -> 随请求头携带 -> 服务端以此去重

http请求:

```
POST /api/payment
Idempotency-Key: a1b2c3d4-uuid
```

服务端逻辑：
```
if redis.exists(key):
    return 缓存的历史响应        # 直接返回，不重复执行
else:
    result = 执行业务逻辑
    redis.setex(key, 24h, result)
    return result
```
Stripe、支付宝等支付 API 均采用此方案

### 防重放
**核心思想**：**防止同一个请求包被拦截后，在有效时间外被恶意重复发送**。它关注的是“**时效性**”和“**请求合法性**”。
**举个例子**：你登录后发起一个转账请求。攻击者截获了这个请求包，虽然没有解开内容，但在几天后把这个一模一样的包重新发给服务器。如果接口没有防重访机制，这笔转账可能会被再次执行。
**典型应用场景**：支付请求、修改密码、任何涉及资金或敏感信息变更的接口。

**如何设计**：
防重放的设计有四层机制，可以按场景需求选择其中一种或多种：
**1.请求指纹+时间窗口**
```
fingerprint = hash(user_id + action + params + timestamp_minute)
if redis.exists(fingerprint):
    return 429 Too Many Requests
redis.setex(fingerprint, 60, 1)
```
在时间窗口内，相同参数的请求只允许一次。
**2.Nonce+时间戳**
```
请求签名 = HMAC(timestamp + nonce + body, secret_key)
```
服务端验证
```
① timestamp 是否在合理范围（如±5分钟）
② nonce 是否已使用过（存 Redis，TTL=10分钟）
③ 签名是否合法
```
**3.令牌桶/滑动窗口限流**
针对高频重复调用（如刷接口、暴力重试）：
```
用户维度：每秒最多 5 次
IP 维度：每分钟最多 100 次
```
**4.业务层去重表**
```
CREATE TABLE request_log (
    request_id VARCHAR(64) PRIMARY KEY,  -- 客户端传入
    user_id    BIGINT,
    created_at DATETIME,
    result     TEXT
);
-- 插入失败（主键冲突）= 重复请求，返回原始结果
```
|方案|防的是什么|典型场景|
|------|------|------|
|1. 请求指纹+时间窗口|用户短时间内重复提交（误触、连击）|表单防重复提交、防误操作|
|2. Nonce+时间戳签名|攻击者截获请求后重放（安全攻击）|开放 API、支付回调、第三方对接|
|3. 令牌桶/限流高频|刷接口（暴力请求）|登录接口防爆破、公开接口防刷|
|4. 业务去重|表业务层面的重复操作|订单、消息、任务等不可重复的业务|

**怎么选**

- **只面向内部用户的普通业务接口**：
方案 1 或 4 选一个就够，防的是正常用户的误操作。

- **对外开放的 API（有第三方调用者）**：
方案 2 是必选，防的是真实的安全威胁；再按需叠加方案 3 做限流。

- **高并发、核心业务（如支付、下单）**：
方案 4（去重表）作为最终兜底，数据库唯一约束是最可靠的保障。

- **公开裸接口（无鉴权）**：
方案 3 是基础门槛，没有限流几乎必然被滥用。


### 幂等与防重放的区别与联系
|维度 |幂等性 (Idempotence) |防重访 (Anti-Replay)|
|------|-------|-------|
|核心目标 |保证操作的最终结果一致 |保证请求的时效性和唯一性|
|关注点 |结果：多次操作结果相同 |请求：同一个请求不被重复执行|
|时间维度 |无时间限制，任何时刻重复执行结果一致 |有明确的时间窗口，过期请求会被拒绝|
|服务端状态 |通常需要存储处理结果或使用乐观锁 |需要存储请求标识（如Nonce、时间戳）|
|典型场景 |订单支付、状态更新、删除操作 |登录、转账、修改密码等敏感操作|

一句话区分：

- 幂等性说的是"你来多少次，结果都一样，我不怕"
- 防重访说的是"你来第二次，我认出你了，直接挡回去"


组合使用的典型场景：支付场景
```
用户点击支付
    │
    ├─→ 防重访检查：1秒内同一用户同一金额只允许发起一次  ──→ 拦截误触/连击
    │
    ├─→ 携带 Idempotency-Key 调用支付接口
    │
    ├─→ 网络超时，客户端重试（携带相同 Key）
    │
    └─→ 幂等性保证：重试不会二次扣款                    ──→ 保证最终一致
```


**防重访在前，幂等性兜底** —— 这是生产系统的标准组合。
